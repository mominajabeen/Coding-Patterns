using namespace std;

#include <iostream>
#include <vector>

class Solution {
public:
  vector<int> findNumbers(vector<int> &nums) {
    vector<int> missingNumbers;
    int i = 0;
    while(i < nums.size()){
      int j = nums[i] - 1;
      if(nums[i] - 1 == i or nums[i] == nums[j])  i++;
    else  swap(nums, i, j);
  }
  for(int i = 0; i < nums.size(); i++){
    if(nums[i] != i + 1)
      missingNumbers.push_back(i + 1);
  }
    // TODO: Write your code here
    return missingNumbers;
  }
private:
  static void swap(vector<int>& arr, int i, int j){
    int temp = arr[i];
    arr[i] = arr[j];
    arr[j] = temp;
  }
};
